export enum Config {
  app = 'app',
  auth = 'auth',
  cache = 'cache',
  db = 'db',
  services = 'services',
  settings = 'settings',
}

export enum Environment {
  Local = 'localhost',
  Development = 'development',
  Production = 'production',
}
